//! Unit tests for the crate.
//!
//! Tests are kept here, in one place per source module, rather than inline in
//! each file. Items under test are reached through their public `crate::…` paths.

mod error;
mod functions;
mod linear_model;
mod metrics;
mod model;
mod preprocessing;
mod regression;
